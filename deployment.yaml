apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: event-app
  template:
    metadata:
      labels:
        app: event-app
    spec:
      containers:
      - name: event-app
        image: anantha06/event-management-qq-one:latest
        ports:
        - containerPort: 8080
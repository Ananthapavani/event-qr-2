spring.application.name=event-management-qr
# App

server.port=8080

# MySQL Database (PERMANENT STORAGE)
# Database Configuration
spring.datasource.url=jdbc:mysql://host.docker.internal:3306/eventdbqr?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Ananthapavani@3124

# JPA - update keeps data, never drops tables
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# Thymeleaf
spring.thymeleaf.cache=false

# Email (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=musiniananthapavani@gmail.com
spring.mail.password=etlegnaitxtgdeci
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true